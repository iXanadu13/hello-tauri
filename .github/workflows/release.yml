name: "release"
run-name: "v${{ inputs.version }}"

on:
  workflow_dispatch:
    inputs:
      version:
        type: string
        description: "App version (x.y.z)"
        required: true
      release_tag: # don't use __VERSION__ here!
        type: string
        description: "Release tag (default: v[x.y.z])"
        required: false
      release_name:
        type: string
        description: "Release name"
        required: true
        default: "App v__VERSION__"
      release_notes:
        type: string
        description: "Release notes"
        required: false
      is_draft:
        type: boolean
        description: "Save as Draft"
        required: true
        default: true
      delete_release:
        type: boolean
        description: "Delete duplicate release"
        required: false
        default: false
      prerelease:
        type: boolean
        description: "Pre-release"
        required: false
        default: false
      enable_sourcemap:
        type: boolean
        description: "Enable sourcemap"
        required: false
        default: false

jobs:
  build:
    permissions:
      contents: write
    uses: ./.github/workflows/publish.yml
    with:
      app_version: ${{ inputs.version }}
      app_version_msi: ${{ inputs.version }}
      release_tag: ${{ inputs.release_tag != '' && inputs.release_tag || format('v{0}', inputs.version) }}
      release_name: ${{ inputs.release_name }}
      release_notes: ${{ inputs.release_notes }}
      is_draft: ${{ inputs.is_draft }}
      prerelease: ${{ inputs.prerelease }}
      enable_sourcemap: ${{ inputs.enable_sourcemap }}
      delete_release: ${{ inputs.delete_release }}
    # secrets:
      